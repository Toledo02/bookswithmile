<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Editar Livro</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-5 mb-5">
    <div class="card shadow">
        <div class="card-header bg-warning">
            <h3>Editar Resenha</h3>
        </div>
        <div class="card-body">
            <form action="/admin/editar/salvar/<%= livro.id %>" method="POST" enctype="multipart/form-data">
                
                <div class="mb-3">
                    <label>Título</label>
                    <input type="text" name="titulo" class="form-control" value="<%= livro.titulo %>" required>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label>Autor</label>
                        <input type="text" name="autor" class="form-control" value="<%= livro.autor %>" required>
                    </div>
                    <div class="col-md-6 mb-3">
                        <label>Nota</label>
                        <input type="number" name="nota" class="form-control" value="<%= livro.nota %>" min="1" max="5" required>
                    </div>
                </div>

                <div class="mb-3">
                    <label>Capa Atual</label><br>
                    <% if(livro.imagem) { %>
                        <img src="<%= livro.imagem %>" width="100" class="mb-2 border">
                    <% } %>
                    <input type="file" name="imagem" class="form-control" accept="image/*">
                    <small class="text-muted">Deixe em branco para manter a imagem atual.</small>
                </div>

                <div class="mb-3">
                    <label>Resumo</label>
                    <textarea name="resumo" class="form-control" rows="2"><%= livro.resumo %></textarea>
                </div>

                <div class="mb-3">
                    <label>Resenha Completa</label>
                    <textarea name="resenha" class="form-control" rows="10"><%= livro.resenha %></textarea>
                </div>

                <div class="mb-3">
                    <label>Link de Compra</label>
                    <input type="url" name="link_compra" class="form-control" value="<%= livro.link_compra %>">
                </div>

                <div class="d-flex justify-content-between">
                    <a href="/admin" class="btn btn-secondary">Cancelar</a>
                    <button type="submit" class="btn btn-primary">Salvar Alterações</button>
                </div>
            </form>
        </div>
    </div>
</div>

</body>
</html>